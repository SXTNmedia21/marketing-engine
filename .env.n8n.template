# n8n MCP — local dev config
# Copy to .env.n8n.local and fill in API_KEY after first n8n boot.
# .env.n8n.local is gitignored. Never commit real keys.

N8N_API_URL=http://localhost:5678/api/v1
N8N_API_KEY=REPLACE_WITH_KEY_FROM_N8N_SETTINGS_API
